BASE_URL=http://localhost:8000

# ==============================================================================
# 🌐 DATABASE (Render) & GOOGLE OAUTH (Google Cloud)
# Both require team Google account credentials — see the `#uni-project` Slack channel.
# ==============================================================================

# --- Database (Render Cloud PostgreSQL) ---
# 1. Go to https://dashboard.render.com/login and sign in with the team Google account.
# 2. Open the `DoseMate` project → select the `main-db` service.
# 3. Under the “Connect” tab, copy the External Database URL.
# 4. Update prefix from `postgresql://` → `postgresql+asyncpg://` **(important!)**
DATABASE_URL=postgresql+asyncpg://myuser:mypass@db.example.com:5432/dosemate_db

# --- Google OAuth (Web App Credentials) ---
# 1. Go to https://console.cloud.google.com/apis/credentials
# 2. Select the OAuth 2.0 Client ID: “DoseMate”
# 3. Copy the Client ID and Secret below.
# 4. Add your ngrok forwarding URL (e.g., https://something.ngrok-free.dev)
#    under “Authorized Redirect URIs”. **(important!)**
GOOGLE_CLIENT_ID=
GOOGLE_CLIENT_SECRET=
GOOGLE_REDIRECT_URI=${BASE_URL}/auth/google/callback

# ==============================================================================

# 🔑 JWT Secret Key — see Slack `#uni-project` channel
JWT_SECRET_KEY=

# 📱 Expo deep link for development
# Printed when running `npm start`
APP_DEEP_LINK_URI=exp://<tunnel_name>/--/auth/callback

# OpenAI API Key — ssee Slack `#uni-peoject` channel
OPENAI_API_KEY=
